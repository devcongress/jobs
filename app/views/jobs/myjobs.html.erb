<% content_for :title do %>
  <%= "My Jobs | #{t('app.name')}" %>
<% end %>
<h1 class="page--title">All My Jobs</h1>
<div class="page--legend-buttons">
  <p class="page--legend">Jobs you have listed are here. Archived jobs are hidden from public view, and have a cream background.</p>
</div>
<div class="main--content">
  <% if @jobs.empty? %>
    <p>You have no jobs listed yet. 🤔</p>
    <% # %p Once a post has been created, you will not be able to delete it. Instead, you will be able to unhide it from public view. This is .  %>
    <%= link_to 'List your first Job', new_job_path %>
  <% else %>
    <%= render 'jobs/my-jobs-list' %>
  <% end %>
</div>
<br/>
